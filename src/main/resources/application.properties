# Default active profile for local development (overridden in AWS via SPRING_PROFILES_ACTIVE=prod)
spring.profiles.active=dev

# Embedded Tomcat HTTP port
server.port=8080

# JWT token lifetime shared across profiles (24 hours)
jwt.expiration=86400000
